# Production build values (public, safe to commit). Override in the host's build variables if needed.
NEXT_PUBLIC_SITE_URL=https://upcomingwave.org
# GA4 measurement ID - public by design (it appears in every page's source); loads only after consent.
NEXT_PUBLIC_GA_ID=G-DHVDJPT2TG
