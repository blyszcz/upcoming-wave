# Archive — not published

Frozen copy of the full page (all 15 sections and every fact) from before the
four-chapter restructure. Rendered only in development at `/archive/` and
`/pl/archive/` (route files use the `.dev.tsx` extension, which production
builds ignore). Nothing here is linked from the site or listed in the sitemap.
